<div class="goal @(IsActive ? "active" : null)">
    <div class="title">Být sexy</div>
    <div class="description">Být nařachaná svině se sixpackem a vlasy jako Ježíš.</div>
    <div class="button">Aktivovat</div>
</div>

@code {
    [Parameter]
    public bool IsActive { get; set; } 
}