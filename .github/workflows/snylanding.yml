name: Deploy static app to Azure Web App

on: 
  push:
    branches: publishlanding
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: windows-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
          
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.2
      with:
        server: waws-prod-am2-545.ftp.azurewebsites.windows.net/site/wwwroot
        username: snylanding\$snylanding
        password: ${{ secrets.FTP_LANDING }}